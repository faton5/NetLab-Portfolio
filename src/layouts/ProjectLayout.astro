---
import BaseLayout from "./BaseLayout.astro";
import type { CollectionEntry } from "astro:content";

interface Props {
  project: CollectionEntry<"projects">;
}

const { project } = Astro.props;
const data = project.data;

// Handle both old and new formats
const title = data.title;
const description = data.description || data.summary || "";
const category = data.category || data.type || "perso";
const tags = data.tags;
const date = data.date;
const image = data.image;
const status = data.status;
const stack = data.stack;
const highlights = data.highlights;

// GitHub link from old or new format
const github = data.github || data.links?.github;
const demo = data.demo || data.links?.demo;
const doc = data.links?.doc;

const categoryLabels: Record<string, string> = {
  network: "🌐 Réseau",
  ops: "⚙️ Ops/DevOps",
  homelab: "🏠 Homelab",
  security: "🔒 Sécurité",
  reseau: "🌐 Réseau",
  securite: "🔒 Cybersécurité",
  systeme: "⚙️ Systèmes",
  scolaire: "🎓 Scolaire",
  perso: "💡 Personnel",
};

const statusLabels: Record<string, string> = {
  en_prod: "✅ En production",
  en_cours: "🔨 En cours",
  planifie: "📋 Planifié",
};
---

<BaseLayout title={`${title} | Portfolio`} description={description}>
  <article class="container">
    <div class="project-hero">
      {image && <img src={image} alt={title} class="project-hero__image" />}

      <a href="/projets" class="btn btn-ghost" style="margin-bottom: 1.5rem;">
        ← Retour aux projets
      </a>

      <h1 class="page-title">{title}</h1>
      <p class="page-description">{description}</p>

      <div class="project-hero__meta">
        <span class="tag">{categoryLabels[category] || category}</span>
        {status && <span class="tag">{statusLabels[status] || status}</span>}
        <span class="tag tag-outline">
          {date.toLocaleDateString("fr-FR", { year: "numeric", month: "long" })}
        </span>
      </div>

      <div class="tags" style="margin-top: 1rem;">
        {tags.map((tag) => <span class="tag tag-outline">{tag}</span>)}
      </div>

      {
        stack && stack.length > 0 && (
          <div style="margin-top: 1.5rem;">
            <h4 style="font-size: 0.875rem; color: var(--text-muted); margin-bottom: 0.5rem;">
              Stack technique
            </h4>
            <div class="tags">
              {stack.map((tech) => (
                <span class="tag">{tech}</span>
              ))}
            </div>
          </div>
        )
      }

      {
        highlights && highlights.length > 0 && (
          <div style="margin-top: 1.5rem; padding: 1rem; background: var(--bg-tertiary); border-radius: var(--radius-md); border-left: 3px solid var(--accent);">
            <h4 style="font-size: 0.875rem; color: var(--accent); margin-bottom: 0.75rem;">
              Points clés
            </h4>
            <ul style="list-style: none; margin: 0; padding: 0;">
              {highlights.map((h) => (
                <li style="color: var(--text-secondary); margin-bottom: 0.5rem; padding-left: 1rem; position: relative;">
                  <span style="position: absolute; left: 0; color: var(--accent);">
                    ›
                  </span>
                  {h}
                </li>
              ))}
            </ul>
          </div>
        )
      }

      {
        (github || demo || doc) && (
          <div class="project-hero__links">
            {github && (
              <a
                href={github}
                target="_blank"
                rel="noopener noreferrer"
                class="btn btn-secondary"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="currentColor"
                >
                  <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
                </svg>
                GitHub
              </a>
            )}
            {demo && (
              <a
                href={demo}
                target="_blank"
                rel="noopener noreferrer"
                class="btn btn-primary"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
                  <polyline points="15 3 21 3 21 9" />
                  <line x1="10" y1="14" x2="21" y2="3" />
                </svg>
                Démo
              </a>
            )}
            {doc && (
              <a
                href={doc}
                target="_blank"
                rel="noopener noreferrer"
                class="btn btn-secondary"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="18"
                  height="18"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                  <polyline points="14 2 14 8 20 8" />
                  <line x1="16" y1="13" x2="8" y2="13" />
                  <line x1="16" y1="17" x2="8" y2="17" />
                </svg>
                Documentation
              </a>
            )}
          </div>
        )
      }
    </div>

    <div class="prose section">
      <slot />
    </div>
  </article>
</BaseLayout>
